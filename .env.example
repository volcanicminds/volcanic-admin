# Backend base URL exposing the admin capability (GET /admin/manifest, /admin/<resource>)
VITE_API_BASE_URL=http://0.0.0.0:2230

# Data source for development: "mock" (in-memory, no backend) | "rest" (real backend)
VITE_ADMIN_SOURCE=mock
